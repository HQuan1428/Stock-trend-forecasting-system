
# Tasks: Project Scaffold

## Implementation Tasks

- [ ] Create base repository structure.
- [ ] Add `AGENTS.md`.
- [ ] Add `README.md` skeleton.
- [ ] Add `requirements.txt`.
- [ ] Create `src/` package.
- [ ] Add `src/__init__.py`.
- [ ] Create `data/` directory.
- [ ] Create `outputs/` directory.
- [ ] Create `tests/` directory.
- [ ] Create `openspec/` directory if it does not exist.
- [ ] Add a minimal placeholder test so `pytest tests/` can run successfully.

## Verification Tasks

- [ ] Run dependency installation:

```bash
pip install -r requirements.txt
pytest tests/
```